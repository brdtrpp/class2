{
  "name": "joinclass",
  "version": "1.0.2",
  "buildNumber": "100",
  "repository": {
    "type": "git",
    "url": "https://github.com/brdtrpp/class2.git"
  },
  "description": "Find and sign-up for classes in your local area.",
  "scripts": {
    "start": "meteor --settings settings-development.json --port $IP:$PORT",
    "test": "meteor deploy btclass.meteor.com --settings settings-development.json",
    "staging": "modulus env set METEOR_SETTINGS \"$(cat settings-development.json)\" -p 'class - Staging' && modulus deploy -p 'class'",
    "production": "modulus env set METEOR_SETTINGS \"$(cat settings-production.json)\" -p 'class' && modulus deploy -p 'class'",
    "galaxy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy www.joinclass.co --settings settings-production.json"
  },
  "dependencies": {
    "meetup-api": "^0.1.29",
    "nbrite": "^0.2.0",
    "node-craigslist": "^0.1.9"
  }
}
